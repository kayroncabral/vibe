extend type Query {
  doctors: [Doctor]!
}

type Doctor implements User {
  name: String!
  sex: Sex!
  birthday: Date!
  crm: String!
}

type Patient implements User {
  name: String!
  sex: Sex!
  birthday: Date!
  federalTaxNumber: String!
}
