extend type Query {
  doctors: [Doctor]!
}

type Doctor implements User {
  name: String!
  gender: Sex!
  birthday: DateTime!
  crm: String!
}

type Patient implements User {
  name: String!
  gender: Sex!
  birthday: DateTime!
  federalTaxNumber: String!
}
